#include <stdio.h>

int main() {
    int N, suma = 0;
    printf("Ingrese un número entero N: ");
    scanf("%d", &N);

    for (int i = 1; i <= N; i++) {
        suma += i;
    }

    printf("La suma es: %d\n", suma);
    return 0;
}
